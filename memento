#!/usr/bin/env python3
"""MEMENTO CLI — Efficient Memory System: SQLite + LLM + Wiki + Network"""
import sys
from pathlib import Path

# Ensure the module is importable (works both when pip-installed and from repo root)
repo = Path(__file__).resolve().parent
if repo.joinpath("memento_cli.py").exists():
    sys.path.insert(0, str(repo))

from memento_cli import main
main()
